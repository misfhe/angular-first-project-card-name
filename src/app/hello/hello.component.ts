import { Component } from "@angular/core";

@Component({
  selector: 'app-hello',
  template: `<span>hello component works</span>`,
})

export class HelloComponent{}    //vì cần gọi ở chỗ khác nên cần export
